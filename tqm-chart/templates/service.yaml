apiVersion: v1
kind: Service
metadata:
  name: {{ include "tqm-chart.fullname" . }}
  labels:
    {{- include "tqm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tqm-chart.selectorLabels" . | nindent 4 }}
