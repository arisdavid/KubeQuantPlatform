apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqs-manager-chart.fullname" . }}
  labels:
    {{- include "sqs-manager-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqs-manager-chart.selectorLabels" . | nindent 4 }}
